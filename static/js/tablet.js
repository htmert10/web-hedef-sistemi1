"use strict";
const SIZE=320,TARGET_MM=170,FRAME_COUNT=3,$=id=>document.getElementById(id);
const video=$("video"),camera=$("camera"),cctx=camera.getContext("2d",{willReadFrequently:true}),overlay=$("overlay"),octx=overlay.getContext("2d"),warped=$("warped"),wctx=warped.getContext("2d");
let stream,socket,corners=[],reference=null,armed=false,busy=false,pending=null,audioContext,analyser,audioData,ambient=.01,lastTrigger=0;
const status=(text,error=false)=>{$("status").textContent=text;$("status").classList.toggle("error",error)};
function connect(){socket=new WebSocket(`${location.protocol==="https:"?"wss":"ws"}://${location.host}/ws`);socket.onopen=()=>{$("connection").textContent="Bağlı"};socket.onclose=()=>{$("connection").textContent="Bağlantı yok";setTimeout(connect,1500)}}
function send(data){if(socket?.readyState===1)socket.send(JSON.stringify(data))}
async function startCamera(){try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:"environment"},width:{ideal:1280}},audio:true});video.srcObject=stream;await video.play();const ratio=video.videoWidth/video.videoHeight;camera.width=900;camera.height=Math.round(900/ratio);overlay.width=camera.width;overlay.height=camera.height;setupAudio();drawLoop();$("corners").disabled=false;status("Kâğıdın dört köşesini sırayla seç: sol üst, sağ üst, sağ alt, sol alt.")}catch(e){status(`Kamera açılamadı: ${e.message}`,true)}}
function drawLoop(){cctx.drawImage(video,0,0,camera.width,camera.height);drawOverlay();requestAnimationFrame(drawLoop)}
function drawOverlay(){octx.clearRect(0,0,overlay.width,overlay.height);octx.fillStyle="#d8ff3e";octx.strokeStyle="#d8ff3e";octx.lineWidth=4;corners.forEach((p,i)=>{octx.beginPath();octx.arc(p.x*overlay.width,p.y*overlay.height,10,0,Math.PI*2);octx.fill();octx.fillText(i+1,p.x*overlay.width+14,p.y*overlay.height)});if(corners.length>1){octx.beginPath();octx.moveTo(corners[0].x*overlay.width,corners[0].y*overlay.height);corners.slice(1).forEach(p=>octx.lineTo(p.x*overlay.width,p.y*overlay.height));if(corners.length===4)octx.closePath();octx.stroke()}}
overlay.onpointerdown=e=>{if(!stream||corners.length>=4)return;const r=overlay.getBoundingClientRect();corners.push({x:(e.clientX-r.left)/r.width,y:(e.clientY-r.top)/r.height});if(corners.length===4){$("reference").disabled=false;status("Köşeler hazır. Hedef temizken referansı kaydet.")}};
$("corners").onclick=()=>{corners=[];reference=null;armed=false;$("reference").disabled=true;$("arm").disabled=true;$("test").disabled=true;status("Köşeleri yeniden seç.")};
function solve(matrix,vector){const n=vector.length;for(let i=0;i<n;i++){let max=i;for(let j=i+1;j<n;j++)if(Math.abs(matrix[j][i])>Math.abs(matrix[max][i]))max=j;[matrix[i],matrix[max]]=[matrix[max],matrix[i]];[vector[i],vector[max]]=[vector[max],vector[i]];const p=matrix[i][i];if(Math.abs(p)<1e-10)throw Error("Köşe geometrisi geçersiz");for(let j=i;j<n;j++)matrix[i][j]/=p;vector[i]/=p;for(let k=0;k<n;k++){if(k===i)continue;const f=matrix[k][i];for(let j=i;j<n;j++)matrix[k][j]-=f*matrix[i][j];vector[k]-=f*vector[i]}}return vector}
function homography(){const rows=[],values=[];const dst=[[0,0],[1,0],[1,1],[0,1]];for(let i=0;i<4;i++){const [u,v]=dst[i],x=corners[i].x*camera.width,y=corners[i].y*camera.height;rows.push([u,v,1,0,0,0,-u*x,-v*x]);values.push(x);rows.push([0,0,0,u,v,1,-u*y,-v*y]);values.push(y)}return solve(rows,values)}
function warp(){const src=cctx.getImageData(0,0,camera.width,camera.height),out=wctx.createImageData(SIZE,SIZE),h=homography();for(let y=0;y<SIZE;y++){const v=y/(SIZE-1);for(let x=0;x<SIZE;x++){const u=x/(SIZE-1),d=h[6]*u+h[7]*v+1,sx=(h[0]*u+h[1]*v+h[2])/d,sy=(h[3]*u+h[4]*v+h[5])/d,ix=Math.max(0,Math.min(camera.width-1,Math.round(sx))),iy=Math.max(0,Math.min(camera.height-1,Math.round(sy))),si=(iy*camera.width+ix)*4,di=(y*SIZE+x)*4;out.data[di]=src.data[si];out.data[di+1]=src.data[si+1];out.data[di+2]=src.data[si+2];out.data[di+3]=255}}return out}
function gray(img){const out=new Uint8Array(SIZE*SIZE);for(let i=0,j=0;i<img.data.length;i+=4,j++)out[j]=Math.round(img.data[i]*.299+img.data[i+1]*.587+img.data[i+2]*.114);return out}
function median(frames){const out=new Uint8Array(frames[0].length),a=frames[0],b=frames[1],c=frames[2];for(let i=0;i<out.length;i++)out[i]=a[i]>b[i]?(b[i]>c[i]?b[i]:Math.min(a[i],c[i])):(a[i]>c[i]?a[i]:Math.min(b[i],c[i]));return out}
function alignment(current){let best={dx:0,dy:0,error:Infinity};for(let dy=-4;dy<=4;dy++)for(let dx=-4;dx<=4;dx++){let sum=0,n=0;for(let y=8;y<SIZE-8;y+=4)for(let x=8;x<SIZE-8;x+=4){const sx=x-dx,sy=y-dy;sum+=Math.abs(reference[y*SIZE+x]-current[sy*SIZE+sx]);n++}if(sum/n<best.error)best={dx,dy,error:sum/n}}return best}
function analyze(current){const shift=alignment(current),diff=new Uint8Array(SIZE*SIZE),samples=[];let offsetSum=0,offsetN=0;for(let y=8;y<SIZE-8;y+=4)for(let x=8;x<SIZE-8;x+=4){offsetSum+=reference[y*SIZE+x]-current[(y-shift.dy)*SIZE+x-shift.dx];offsetN++}const offset=offsetSum/offsetN;for(let y=2;y<SIZE-2;y++)for(let x=2;x<SIZE-2;x++){const i=y*SIZE+x,v=Math.abs(reference[i]-Math.max(0,Math.min(255,current[(y-shift.dy)*SIZE+x-shift.dx]+offset)));diff[i]=v;if((x+y)%7===0)samples.push(v)}const mean=samples.reduce((a,b)=>a+b,0)/samples.length,sd=Math.sqrt(samples.reduce((a,b)=>a+(b-mean)**2,0)/samples.length),sens=[3,2.5,2][Number($("sensitivity").value)],threshold=Math.max(9,mean+sd*sens),mask=new Uint8Array(diff.length);for(let y=2;y<SIZE-2;y++)for(let x=2;x<SIZE-2;x++){const i=y*SIZE+x,g=Math.abs(reference[i+1]-reference[i-1])+Math.abs(reference[i+SIZE]-reference[i-SIZE]);if(diff[i]>=threshold&&(g<55||diff[i]>38))mask[i]=1}const clean=new Uint8Array(mask.length);for(let y=2;y<SIZE-2;y++)for(let x=2;x<SIZE-2;x++){const i=y*SIZE+x;if(!mask[i])continue;let n=0;for(let yy=-1;yy<=1;yy++)for(let xx=-1;xx<=1;xx++)n+=mask[(y+yy)*SIZE+x+xx];if(n>=3)clean[i]=1}return components(clean,diff,shift.error)}
function components(mask,diff,alignError){const seen=new Uint8Array(mask.length),queue=new Int32Array(mask.length),items=[],diameterPx=Number($("minSize").value)/TARGET_MM*SIZE,minArea=Math.max(5,Math.PI*(diameterPx/2)**2*.28),maxArea=Math.max(220,minArea*18);for(let start=0;start<mask.length;start++){if(!mask[start]||seen[start])continue;let head=0,tail=0,area=0,wx=0,wy=0,weight=0,minX=SIZE,maxX=0,minY=SIZE,maxY=0;queue[tail++]=start;seen[start]=1;while(head<tail){const i=queue[head++],y=Math.floor(i/SIZE),x=i-y*SIZE,w=Math.max(1,diff[i]);area++;wx+=x*w;wy+=y*w;weight+=w;minX=Math.min(minX,x);maxX=Math.max(maxX,x);minY=Math.min(minY,y);maxY=Math.max(maxY,y);for(let yy=-1;yy<=1;yy++)for(let xx=-1;xx<=1;xx++){const ni=(y+yy)*SIZE+x+xx;if(xx===0&&yy===0||ni<0||ni>=mask.length||seen[ni]||!mask[ni])continue;seen[ni]=1;queue[tail++]=ni}}if(area<minArea||area>maxArea)continue;const bw=maxX-minX+1,bh=maxY-minY+1,aspect=bw/bh,fill=area/(bw*bh);if(aspect<.35||aspect>2.85||fill<.14)continue;const sizeScore=Math.min(1,area/(minArea*2)),shapeScore=Math.min(aspect,1/aspect)*.55+Math.min(1,fill/.55)*.45,confidence=Math.max(0,Math.min(1,.42*sizeScore+.42*shapeScore+.16*Math.max(0,1-alignError/18)));items.push({x:wx/weight/(SIZE-1),y:wy/weight/(SIZE-1),area,confidence})}items.sort((a,b)=>b.confidence-a.confidence);return items}
async function captureStable(){const frames=[];let preview;for(let i=0;i<FRAME_COUNT;i++){if(i)await new Promise(r=>setTimeout(r,80));preview=warp();frames.push(gray(preview))}return{preview,current:median(frames)}}
function drawCandidate(candidate,preview){wctx.putImageData(preview,0,0);wctx.strokeStyle="#168cff";wctx.lineWidth=4;wctx.beginPath();wctx.arc(candidate.x*SIZE,candidate.y*SIZE,12,0,Math.PI*2);wctx.stroke()}
async function detect(){if(busy||!reference)return;busy=true;try{status("Üç kare karşılaştırılıyor…");const data=await captureStable(),items=analyze(data.current),best=items[0],second=items[1];if(!best){status("Yeni, yeterli boyutta bir delik bulunamadı.");return}pending={...best,preview:data.preview,current:data.current};drawCandidate(best,data.preview);const unique=!second||best.confidence-second.confidence>.08;if(best.confidence>=.74&&unique){acceptPending();status(`Atış gönderildi · güven %${Math.round(best.confidence*100)}`)}else{$("review").classList.remove("hidden");status(`Değişim bulundu · güven %${Math.round(best.confidence*100)} · kontrol et.`)}}catch(e){status(`Algılama hatası: ${e.message}`,true)}finally{busy=false}}
function acceptPending(){if(!pending)return;send({type:"shot",x:pending.x,y:pending.y,confidence:pending.confidence,source:"camera"});reference=pending.current;wctx.putImageData(pending.preview,0,0);pending=null;$("review").classList.add("hidden")}
function setupAudio(){audioContext=new AudioContext();const source=audioContext.createMediaStreamSource(stream);analyser=audioContext.createAnalyser();analyser.fftSize=1024;audioData=new Uint8Array(analyser.fftSize);source.connect(analyser);const loop=()=>{analyser.getByteTimeDomainData(audioData);let sum=0;for(const v of audioData)sum+=((v-128)/128)**2;const rms=Math.sqrt(sum/audioData.length);if(!armed)ambient=ambient*.98+rms*.02;const now=Date.now();if(armed&&rms>Math.max(.06,ambient*4)&&now-lastTrigger>1600){lastTrigger=now;detect()}requestAnimationFrame(loop)};loop()}
$("reference").onclick=async()=>{try{const data=await captureStable();reference=data.current;wctx.putImageData(data.preview,0,0);$("arm").disabled=false;$("test").disabled=false;status("Temiz referans hazır. Algılamayı başlatabilirsin.")}catch(e){status(e.message,true)}};
$("arm").onclick=()=>{armed=!armed;$("arm").textContent=armed?"Algılamayı durdur":"4 · Algılamayı başlat";status(armed?"Sistem hazır; atış sesi bekleniyor.":"Algılama durduruldu.")};$("test").onclick=detect;$("accept").onclick=()=>{acceptPending();status("Değişim atış olarak kaydedildi.")};$("reject").onclick=()=>{pending=null;$("review").classList.add("hidden");status("Değişim reddedildi; referans korunuyor.")};$("minSize").oninput=e=>$("sizeLabel").textContent=`${Number(e.target.value).toFixed(1)} mm`;$("sensitivity").oninput=e=>$("sensitivityLabel").textContent=["Düşük","Normal","Yüksek"][e.target.value];$("start").onclick=startCamera;connect();
